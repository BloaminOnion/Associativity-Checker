function checkAssociativeProperty (group_table)
  
  # Determines if the group that produces the provided group table
  # is associative, i.e. (a*b)*c = a*(b*c) for all a,b,c in the group.
  
  
  
  
  
endfunction